# Unsupervised Model Artifacts

Training writes firewall anomaly and clustering artifacts here.

The backend expects:

- `anomaly_model.joblib`
- `clustering_model.joblib`

Generated artifacts are ignored by Git by default. Train locally or provide artifacts through deployment storage.
