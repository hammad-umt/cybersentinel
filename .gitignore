# Environment and secrets
**/.env
!**/.env.example

# Python virtual environments
**/.venv/
**/venv/

# Python generated files
**/__pycache__/
*.py[cod]
*.pyo

# Local databases and runtime outputs
*.db
*.sqlite
*.sqlite3
logs/
uploads/

# PyInstaller / desktop packaging outputs (never commit)
cybersentinel-backend/dist/
cybersentinel-backend/build/
packaging/**/dist/
packaging/**/build/
packaging/**/output/
packaging/**/*.exe

# Test/type/lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Datasets and trained ML artifacts are tracked for this project.
# Large CSV/model binaries are routed through Git LFS in .gitattributes.

# Flutter / Dart client at repo root (separate from backend scope)
/linux/
/windows/

# Editor / local tooling
.cursor/

# OS/editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
